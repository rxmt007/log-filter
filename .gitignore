# ── 原 Java 参考工程(新工程成型后删除)──
/LogFilter/

# ── Rust / Cargo ──
/target/
**/target/
# 注:应用程序应提交 Cargo.lock,故不忽略

# ── Tauri v2 ──
/src-tauri/target/
/src-tauri/gen/

# ── 前端 / Node ──
node_modules/
dist/
dist-ssr/
.vite/
*.local

# ── 环境变量 / 密钥 ──
.env
.env.*
!.env.example

# ── 编辑器 / IDE ──
.idea/
.vscode/*
!.vscode/extensions.json

# ── 操作系统 ──
.DS_Store
Thumbs.db
desktop.ini

# ── 日志 / 临时 ──
*.log
